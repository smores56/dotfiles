#!/bin/sh

theme=$(cat ~/.theme 2>/dev/null)
if [ "$theme" = "light" ] || [ "$theme" = "dark" ]; then
    echo $theme
else
    currentTime=$(date "+%H")
    if [ $currentTime -ge 7 ] && [ $currentTime -lt 18 ]; then
        echo light
    else
        echo dark
    fi
fi

