# workflow apps
abbr -a e "kak"
abbr -a l "exa -lh"
abbr -a t "tmux-smart"
abbr -a f "ranger"
abbr -a c "bat"
abbr -a g "lazygit"
abbr -a / "fd --type f"

# dotfiles management
abbr -a cm "chezmoi"
abbr -a ca "chezmoi apply"
abbr -a df "lazygit -p ~/.local/share/chezmoi/"

# remote access
abbr -a lpk "ssh root@lpk.wiki"
abbr -a hom "ssh-tmux smores@home.mohr.codes"
abbr -a doc "ssh-tmux root@dev.mohr.codes"
abbr -a pfhom "port-forward smores@home.mohr.codes"
abbr -a pfdoc "port-forward root@dev.mohr.codes"
